= render './shared/header'
%h1 メーカー一覧
- @categories.each do |category|
  %section.manufacturer-wrapper
    %ul
      %li
        -# = link_to category do
        %img{src: "#{category.image}", :width => "100"}/
      %li
        = category.name
